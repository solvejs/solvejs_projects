import React, { Component } from 'react';
import ReactDOM, { render } from 'react-dom';

class TestDiv extends Component {
        render() {
            return (
                <button className = 'submitTest'>button</button>
            )
        }
}

/* ReactDOM.render(
    <Button />,
    document.getElementById('root')
) */

export default TestDiv;